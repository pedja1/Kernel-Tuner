<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent"
	android:orientation="vertical"
	android:padding="10dp">

	<CheckBox
		android:layout_height="wrap_content"
		android:layout_width="match_parent"
		android:id="@+id/cbEnableAll"
		android:text="Enable all CPU cores when entering CPU settings"/>

	<CheckBox
		android:layout_height="wrap_content"
		android:layout_width="match_parent"
		android:id="@+id/cbDisableAllOnExit"
		android:text="Disable all CPU cores(except core0) when leaving CPU settings"
		android:layout_marginTop="5dp"/>

	<TextView
		android:layout_height="wrap_content"
		android:text="Note: In order to change settings for each core, all cores needs to be enabled"
		android:textAppearance="?android:attr/textAppearanceSmall"
		android:layout_width="wrap_content"
		android:layout_marginTop="5dp"/>

	<CheckBox
		android:layout_height="wrap_content"
		android:layout_width="match_parent"
		android:id="@+id/cbSave"
		android:text="Save and don't ask again"
		android:layout_marginTop="20dp"/>

</LinearLayout>

